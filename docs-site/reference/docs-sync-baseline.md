# 文档同步基线

这页专门用来回答一个很实际的问题：

**当前这套文档，究竟同步到了哪一个 Git 提交。**

如果没有这页，后面很容易出现两种误判：

1. 文档看起来很完整，但其实只覆盖到几次提交之前。
2. 大家口头说“已经同步了”，却说不清到底同步到了哪个 commit。

## 先说结论

**不要只记“git log 第几个”。**

更稳的做法是同时记录：

| 字段 | 为什么需要 |
| --- | --- |
| 分支名 | 同一个仓库不同分支的文档状态可能完全不同 |
| 提交 SHA | 最稳定的唯一标识 |
| 提交时间 | 方便判断是不是最新一批 |
| 提交标题 | 方便人快速读懂这次同步的大致内容 |
| `git rev-list --count HEAD` | 可以作为“第几个提交”的辅助序号，但不能单独依赖 |

其中真正可靠的是 **SHA**，不是“第几个”。

## 当前同步基线

下面这组信息表示：**当前文档站内容已至少同步到这个提交。**

| 项 | 当前值 |
| --- | --- |
| 同步分支 | `v2.2.0` |
| 提交短 SHA | `1ad6202` |
| 提交完整 SHA | `1ad6202756d76f10ab18f5d7ae1d440a698a6b59` |
| Git 提交序号（辅助值） | `225` |
| 提交时间 | `2026-05-18 11:28:09 +0800` |
| 提交标题 | `docs: add module owner playbook` |
| 文档状态 | 已覆盖到“模块值守与回归手册”这一批 |

## 这页该怎么判断“文档是不是落后了”

最简单的判断方式就是对比当前仓库 `HEAD`：

```powershell
git rev-parse HEAD
git rev-list --count HEAD
git log -1 --date=iso --pretty=format:"%H%n%h%n%cd%n%s"
```

如果你跑出来的结果和本页不一致，有三种常见情况：

| 情况 | 说明 |
| --- | --- |
| SHA 一致 | 当前文档和代码基线一致 |
| SHA 不一致，但只是代码多了新提交 | 文档可能滞后，需要补同步 |
| 分支不同 | 先确认你看的到底是不是同一条分支的文档 |

## 为什么“第几个提交”不能单独当基线

因为它只是一个辅助阅读值，不是稳定 ID。

举个例子：

1. 你今天看到是第 `225` 个提交。
2. 明天有人 `rebase`、整理历史、切到别的分支，序号可能变化。
3. 但 `1ad6202756d76f10ab18f5d7ae1d440a698a6b59` 这个 SHA 只指向这一条提交。

所以更稳的规则应该是：

- **对机器**：看 SHA
- **对人**：看标题、时间和辅助序号

## 推荐更新规则

以后每次文档做完一个明显批次，建议同步更新这页。

推荐顺序：

1. 完成文档修改。
2. 跑文档站构建。
3. 提交本次文档改动。
4. 用最新 commit 刷新这页里的基线信息。

最低建议一起更新的地方有：

| 页面 | 为什么一起更新 |
| --- | --- |
| 本页 | 标识当前文档同步到了哪个 commit |
| [文档维护规范](/guide/documentation-maintenance) | 保证团队知道这一步是固定动作 |
| [v2.2.0 文档计划](/roadmap/v2.2.0-docs-plan) | 记录这次补了哪一批内容 |

## 最小维护模板

如果你以后想继续沿用这套方式，最小模板就是：

```text
同步分支：
提交短 SHA：
提交完整 SHA：
Git 提交序号（辅助值）：
提交时间：
提交标题：
本批覆盖内容：
```

这套模板的优点是：

1. 人能看懂。
2. 机器能核对。
3. 以后要追“文档是从哪一批开始有这部分内容的”也容易查。

## 和现有文档体系怎么配合

这页不是替代：

- [文档维护规范](/guide/documentation-maintenance)
- [验证记录与已知问题](/manuals/verified-scenarios)
- [v2.2.0 文档计划](/roadmap/v2.2.0-docs-plan)

它的职责更单一：

**只负责标记“当前文档同步基线在哪里”。**

你可以把它理解成这套文档站的“对齐刻度线”。
