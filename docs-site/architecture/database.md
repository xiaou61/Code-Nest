# 数据库与脚本

数据库脚本放在 `sql/`，采用“基线 + 版本增量”的方式组织。

## 目录结构

| 路径 | 说明 |
| --- | --- |
| `sql/MySql/code_nest.sql` | 当前完整结构基线 |
| `sql/MySql/code_nest_data.sql` | 当前初始化数据 |
| `sql/v1.2.0/` 到 `sql/v1.8.4/` | 历史增量脚本 |

## 常见脚本域

| 目录 | 代表能力 |
| --- | --- |
| `v1.2.1` | 敏感词、知识图谱 |
| `v1.3.0` | 版本历史、程序员日历、Bug 商店 |
| `v1.4.0` | 积分体系、聊天室 |
| `v1.5.0` | 博客、社区、动态、抽奖、敏感词升级 |
| `v1.6.0` | 简历、代码工坊 |
| `v1.6.1` | 计划打卡 |
| `v1.6.3` | 模拟面试 |
| `v1.7.0` | 面试学习记录、聊天室升级、学习追踪 |
| `v1.7.1` | 学习小组 |
| `v1.7.2` | 闪卡 |
| `v1.8.0` | OJ 题目、判题、题解 |
| `v1.8.1` | OJ 赛事 |
| `v1.8.2` | 求职闭环、学习驾驶舱、SQL 优化、OJ 评论 |
| `v1.8.4` | 学习资产转化引擎 |

## 新增脚本原则

1. 结构变更写入版本增量目录，不直接覆盖历史脚本。
2. 表名和字段使用 `snake_case`。
3. 默认字符集保持 `utf8mb4`。
4. 线上兼容优先，谨慎使用破坏性 `DROP`。
5. 初始化数据和结构变更拆清执行顺序。
6. 新增功能文档要写清涉及的表、核心索引和数据生命周期。

## 脚本检查清单

提交数据库变更前，至少确认：

1. 基线脚本和版本增量脚本的职责没有混用。
2. 新表使用 `utf8mb4`，字段命名保持 `snake_case`。
3. 增量脚本能在已有库上重复评审，不依赖手工改表。
4. 初始化数据和结构脚本执行顺序清楚。
5. 对应模块文档和 [数据表索引](/reference/database-tables) 已同步更新。
