<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.sre.mapper.SreRcaEvaluationRunCaseMapper">

    <resultMap id="SreRcaEvaluationRunCaseResult" type="com.xiaou.sre.domain.SreRcaEvaluationRunCase">
        <id property="id" column="id"/>
        <result property="evaluationRunId" column="evaluation_run_id"/>
        <result property="caseId" column="case_id"/>
        <result property="caseOrdinal" column="case_ordinal"/>
        <result property="caseContentSha256" column="case_content_sha256"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insertBatch">
        INSERT INTO sre_rca_evaluation_run_case (
            evaluation_run_id, case_id, case_ordinal, case_content_sha256, create_time
        ) VALUES
        <foreach collection="members" item="member" separator=",">
            (#{member.evaluationRunId}, #{member.caseId}, #{member.caseOrdinal},
             #{member.caseContentSha256}, CURRENT_TIMESTAMP)
        </foreach>
    </insert>

    <select id="selectByRunId" resultMap="SreRcaEvaluationRunCaseResult">
        SELECT id, evaluation_run_id, case_id, case_ordinal, case_content_sha256, create_time
        FROM sre_rca_evaluation_run_case
        WHERE evaluation_run_id = #{runId}
        ORDER BY case_ordinal ASC, id ASC
    </select>

    <select id="countByRunIdAndCaseId" resultType="int">
        SELECT COUNT(*)
        FROM sre_rca_evaluation_run_case
        WHERE evaluation_run_id = #{runId}
          AND case_id = #{caseId}
    </select>
</mapper>
