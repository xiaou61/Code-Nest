<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.team.mapper.StudyTeamDiscussionLikeMapper">

    <insert id="insert">
        INSERT INTO study_team_discussion_like (discussion_id, user_id, create_time)
        VALUES (#{discussionId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM study_team_discussion_like
        WHERE discussion_id = #{discussionId} AND user_id = #{userId}
    </delete>

    <select id="checkExists" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM study_team_discussion_like
        WHERE discussion_id = #{discussionId} AND user_id = #{userId}
    </select>

    <select id="selectLikedDiscussionIds" resultType="java.lang.Long">
        SELECT discussion_id
        FROM study_team_discussion_like
        WHERE user_id = #{userId}
        <if test="discussionIds != null and discussionIds.size() > 0">
            AND discussion_id IN
            <foreach collection="discussionIds" item="discussionId" open="(" separator="," close=")">
                #{discussionId}
            </foreach>
        </if>
    </select>

</mapper>
