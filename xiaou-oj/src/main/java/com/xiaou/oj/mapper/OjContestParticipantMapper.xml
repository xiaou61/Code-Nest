<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.oj.mapper.OjContestParticipantMapper">

    <resultMap id="BaseResultMap" type="com.xiaou.oj.domain.OjContestParticipant">
        <id column="id" property="id"/>
        <result column="contest_id" property="contestId"/>
        <result column="user_id" property="userId"/>
        <result column="join_time" property="joinTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO oj_contest_participant (contest_id, user_id, join_time)
        VALUES (#{contestId}, #{userId}, NOW())
    </insert>

    <select id="existsByContestIdAndUserId" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM oj_contest_participant
            WHERE contest_id = #{contestId} AND user_id = #{userId}
        )
    </select>

    <select id="selectByContestId" resultMap="BaseResultMap">
        SELECT * FROM oj_contest_participant
        WHERE contest_id = #{contestId}
        ORDER BY id ASC
    </select>

    <select id="countByContestId" resultType="int">
        SELECT COUNT(*) FROM oj_contest_participant WHERE contest_id = #{contestId}
    </select>

    <delete id="deleteByContestId">
        DELETE FROM oj_contest_participant WHERE contest_id = #{contestId}
    </delete>
</mapper>
